import { StyleSheet } from 'react-native';
import { colors, radius } from '@/design_system/ui/tokens';

export const styles = StyleSheet.create({
  skeleton: {
    backgroundColor: '#E1E9EE',
    borderRadius: radius.xs,
  },
});